{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "vectorSearch/components/fields.json",
  "type": "array",
  "properties": {
    "type": {
      "type": "string",
      "enum": ["vector", "filter"],
      "markdownEnumDescriptions": [
        "Field type to use to index array of numbers for vector search. To learn more, see [vector Type](https://dochub.mongodb.org/core/avs-vector-type/).",
        "Field type to use to index string, number, and boolean values for pre-filtering the data. To learn more, see [filter Type](https://dochub.mongodb.org/core/avs-vector-search-filter-type/)."
      ]
    }
  },
  "minItems": 1,
  "items": {
    "type": "object",
    "oneOf": [
      {
        "$ref": "fields/vector.json"
      },
      {
        "$ref": "fields/filter.json"
      }
    ]
  },
  "contains": {
    "properties": {
      "type": {
        "const": "vector"
      }
    }
  },
  "additionalProperties": false,
  "definitions": {
    "typeErrorMessage": {
      "errorMessage": "Unsupported vector field type.",
      "$comment": "The custom error message defined above is a global override for all consuming schema."
    }
  }
}
