"""MODULE.bazel for prometheus."""

tools_repos = use_extension("//bazel/tools:extensions.bzl", "tools_repos")
use_repo(
    tools_repos,
    "prometheus_linux_aarch64",
    "prometheus_linux_x86_64",
    "prometheus_macos_aarch64",
    "prometheus_macos_x86_64",
)
